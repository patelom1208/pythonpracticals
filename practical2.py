tuple = (1,7,8,9,6)
print (tuple)

sum_tup = sum(tuple)
print("SUM OF TUPLE ",[sum_tup])
max_tup = max(tuple)
print("Maximum number ",[max_tup])

min_tup = min(tuple)
print("Minimum number",[min_tup])

len_tup = len(tuple)
print ("Length of tuple",[len_tup])

average_tup = sum_tup/len_tup
print ("Average of tuple",[average_tup])

addition_tup = tuple[0] + tuple [1]
print ("Addition of first and second element", [addition_tup])

#Lists are mutable and can be changed after creation. Tuples are immutable they cannot be changed once created.
